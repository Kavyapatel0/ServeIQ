# Base URL of the ServeIQ backend API (no trailing slash, no /api suffix).
# Leave unset in local dev — Vite's proxy in vite.config.js forwards
# /api/* to http://localhost:5000 automatically.
# VITE_API_URL=https://api.yourdomain.com
